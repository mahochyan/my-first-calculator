# Python 函数 (Functions)

当你需要重复执行一段相同代码时，你会把它封装在一个函数 (Function) 里。

### 学习内容
1.  **函数定义 (`def`)**: 给代码块起一个名字。
2.  **函数调用**: 用名字执行代码。

### 毕业练习 (Graduation Project)
在这个阶段，我们将之前在仓库根目录下的 `calculator.py` 移到了这里。这是一个经典的计算器案例，展示了如何通过不同的函数来实现不同的计算功能。

### 运行示例
```bash
python3 calculator.py
```
