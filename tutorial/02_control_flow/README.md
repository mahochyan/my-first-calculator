# Python 控制流 (Control Flow)

这一步你将学习如何让 Python 程序根据条件做决策，以及如何重复执行某些任务。

### 学习内容
1.  **条件语句 (`if`, `elif`, `else`)**: 程序的分叉路口。
2.  **循环语句 (`for`, `while`)**: 重复执行一段代码。

### 运行示例
```bash
python3 control_flow.py
```
